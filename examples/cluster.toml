# Optional Raft tuning (defaults shown). Omit [raft] to use these values.
# [raft]
# epoch_interval_ms = 50
# election_timeout_epochs = 30   # must be >= 4 * heartbeat_interval_epochs
# heartbeat_interval_epochs = 1  # ~50 ms with epoch_interval_ms = 50
# election_jitter_epochs = 3
# audit_interval_epochs = 100
# compact_threshold = 100
# inter_server_timeout_ms = 50   # one heartbeat period

[cluster]
servers = 4

[[nodes]]
id = 1
host = "127.0.0.1"
port = 9101
audit_port = 9401

[[nodes]]
id = 2
host = "127.0.0.1"
port = 9102
audit_port = 9402

[[nodes]]
id = 3
host = "127.0.0.1"
port = 9103
audit_port = 9403

[[nodes]]
id = 4
host = "127.0.0.1"
port = 9104
audit_port = 9404
